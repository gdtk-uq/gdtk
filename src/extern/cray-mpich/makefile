# makefile for cray-mpi, mostly because the eilmer make process expects one
# In the future, we could run an automated process for generating mpi.d here.
# NNG, 14/06/22

setup:
	echo "Setup cray-mpich"

clean:
	echo "Cleaned cray-mpich"

